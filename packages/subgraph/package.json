{
  "name": "@livepeer/subgraph",
  "private": true,
  "version": "1.0.0-alpha.5",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir types/ subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "build-ipfs": "graph build --ipfs /ip4/127.0.0.1/tcp/5001 subgraph.yaml",
    "build-wast": "graph build -t wast subgraph.yaml",
    "deploy": "graph deploy livepeer subgraph.yaml --verbosity debug --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001/ --access-token x"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "https://github.com/graphprotocol/graph-cli.git",
    "@graphprotocol/graph-ts": "0.5.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "@graphprotocol/graph-cli",
      "@graphprotocol/graph-ts"
    ]
  }
}
